mixin stars(id, activeIndex)
  .stars
    each item, index in [1, 2, 3, 4, 5]
      input(type='checkbox', id=id+item, name='', value=item, checked=index === activeIndex)
      label(for=id+item)
        +svg('star')
