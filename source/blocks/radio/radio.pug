mixin radio(data, classMod)
  .radio(class=classMod || '')
    input(type='radio', name=data.name, value=data.value, id=data.id)
    label(for=data.id)
      span!=data.label
      if data.info
        span.radio__info!= data.info
